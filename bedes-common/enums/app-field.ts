/**
 * Enumerated values for app fields, should correspond
 * to id's in the public.app_field table
 */
export enum AppField {
    DataType=1,
    Units,
    Source,
    Table,
    Group,
    Notes,
    FieldCode,
    EnumeratedValue
}