/**
 * Defines values the enumerated values for an mapping applications scope,
 * ie if the application is visible (Public) or not (Private) to the outside world.
 */
export enum ApplicationScope {
    Private=1,
    Public=3,
    Approved=4,
}
