import { Router } from 'express';
// import * as handlers from './handlers';

function mountRoutes(router: Router) {
    // router.get('/utility/:id/location', handlers.getUtilityLocations);
    // router.get('/utility/:id/:fromDate/:toDate/:maxPoints', handlers.getUtilityDataByDate);
    // router.get('/utility/:id/:fromDate/:toDate', handlers.getUtilityDataByDate);
    // router.get('/utility/:id', handlers.getUtilityDetails);
    // router.get('/utility', handlers.getUtilityList);

    // router.post('/location/:id', handlers.getLocationData);
    // router.get('/location/:id/events', handlers.getLocationEventData);
    // router.get('/location/:id/:fromDate/:toDate/:maxPoints/:daysOfWeek/:hoursOfDay', handlers.getLocationData);
    // router.get('/location/:id/:fromDate/:toDate/:maxPoints/:daysOfWeek', handlers.getLocationData);
    // router.get('/location/:id/:fromDate/:toDate/:maxPoints', handlers.getLocationData);

    // router.get('/events/:id', handlers.getOutageEventData);
}

export {
    mountRoutes
}
